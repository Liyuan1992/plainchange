# Changelog

本项目遵循语义化版本；Alpha 版本仍可能调整界面和数据合同。

## Unreleased

- 修复首次使用页读取零提交或仅一次提交项目时暴露 Git 原始错误的问题；现在分别说明“还没有保存过版本”和“只有一个已保存版本”，并告诉用户如何形成可比较的前后版本，不会修改目标项目。
- 新增 Windows 便携式首次使用路径：构建 `PlainChange.exe` 后，用户可解压并双击打开本地图形页面，不必安装 Python、uv 或编辑 JSON。首次使用页改为直接填写接口地址、模型名与掩码 API Key；密钥仅在当前进程内存中使用，不进入报告、日志、配置或 Git。CLI 的环境变量配置仍保留给自动化。
- 用真实业务项目回归模型负责人语言合同，并修复通用首屏投影缺陷：首屏只显示受约束的结果式短标题，完整解释仍保留在下方；最终样本保持六项以内业务地图、具体角色仅标为“可能受影响”、运行时限制不变。
- 收敛模型优先报告的负责人语言合同：业务总览最多六项，标题必须先说明结果并受长度限制；技术实现继续留在证据下钻。模型可提出带来源的具体角色候选，界面只标为“可能受影响”，不会把它升级成已确认用户影响。
- 首次通过真实 OpenAI-compatible 接口完成两阶段业务项目分析；新增受证据约束的项目/变更输入压缩，避免把 622 KB 完整证据包直接发送给模型，并由父级结构归一冗余能力类型。ChestnutDogAiThink 样本已从异常分支误聚焦转为商品推荐总览主线，运行与用户影响仍保持未验证。
- 将完整产品路径改为模型优先的两阶段理解：固定版本的项目理解可按输入/模型身份缓存，再结合变更证据生成负责人说明；确定性校验仍控制来源、事实状态、未知项和降级。
- 浏览器入口新增通用 OpenAI-compatible 模型设置，只填写密钥环境变量名；无模型时明确降级为“基础证据模式”，不再把规则摘要包装成完整业务理解。
- 通用源码范围排除 `node_modules`、`vendor`、构建输出等依赖/生成目录，并补充 Vue、MJS、CJS 静态模块覆盖与 Markdown 表格语义列选择。

- 补齐英文变化页技术详情的语言投影，并把浏览器验收扩展到展开任务上下文、切换 Before/After/Changes only 和逐个点击节点；不再以默认首屏代替隐藏交互状态的本地化检查。
- 修复英文能力图仍把中文“能”字当作视觉图标的问题，并新增可选的真实浏览器可见中文扫描；纯英文 FastAPI 固定版本样本的变化页、系统页和技术展开区均为 0 条可见中文。
- 补齐技术架构页的语言无关投影：概念图标题、边界、候选快照状态和自动实现分类从稳定状态/ID 生成英文；项目能力原文与技术证据仍保持来源语言。
- 将 PlainChange 自动生成的负责人结论从中文成句改为语言无关的消息键与参数，再分别渲染中英文；动态数量、文件名、代码结构候选职责和证据状态无需项目专用词条。项目说明与项目能力名称仍保留来源语言，完整审阅译文包可以覆盖自动英文。
- 修复技术实现结构在宽内容下被裁切且无法横向滚动的问题；滚动现在由内部架构画布承接，不会让整份报告产生横向溢出。
- 能力图支持证据约束的内部职责层：只有在能力明确引用的代码范围内识别出至少两个不同职责时才可原位展开；证据不足时保持叶子节点，不再复制父节点或用宽松名称匹配制造假层级。
- 修复无可靠顺序流程时仍显示通用四步箭头的问题：从项目说明的能力清单/状态表提炼无先后顺序的能力图，缺少说明时只展示代码实现区域；有明确流程的项目继续保留流程图。
- 补齐来源身份绑定的报告正文译文：摘要、流程节点、展开详情和右侧说明同步切换；新增 `localize-report` 导出/应用译文包，拒绝缺项或过期译文，保留原始证据。
- 将公开入口拆分为英文 `README.md` 与简体中文 `README.zh-CN.md`。
- `review.html` 新增仅限中英文的离线语言切换：首次跟随浏览器/系统语言，手动选择保存在本机，其他语言回退英文。
- 界面翻译不改写嵌入证据；项目说明、任务原话、技术证据和只提供单语的解释正文继续保留来源语言。

## 0.1.0a1 - 2026-09-10

PlainChange 的首个可邀请试用本地 Alpha；生成的核心产物继续叫 Change Passport：

- 把固定 Git 变化生成“这次改了什么 / 这个软件怎么工作”两屏报告。
- 保留已确认、目前没发现、还没验证三类状态，不把静态线索冒充运行事实。
- 支持项目说明与代码位置分别标注，自动生成候选项目分区和工作流程。
- 支持大仓库的对象预检、受管补齐、阶段进度、解析缓存与技术数据按需加载。
- 支持可选 OpenAI-compatible 模型提供商，默认不调用模型。
- 新增 `plainchange analyze .` 与 `plainchange .`：直接分析当前 Git 项目。
- 新增 `plainchange serve` 首次使用页面：选择项目和前后版本即可生成报告。
- 明确品牌与产物分离：PlainChange 是产品，Change Passport 是可验证的变化产物与协议。

仍未完成：独立非技术用户理解评估、三份正式评分样本、批准 baseline、原生安装程序和公开发布验证。
