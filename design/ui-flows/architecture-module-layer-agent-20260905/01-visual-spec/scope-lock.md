# Scope Lock

## Reference Exists And Current Business Exists: Must Implement

- Existing selected section title, 227-module total, module inspection, and return-to-system action.
- A deterministic, complete path from all section nodes to visible reading-area counts.
- Progressive disclosure so a beginner can stop before any source identifier appears.

## Reference Exists But Current Business Does Not Exist: Do Not Fake, Pending User Confirmation

- None. The screenshot is the current product state, not a speculative mockup.

## Current Page Exists But Reference Does Not Show It: Hide Or Downplay This Task

- Raw three-column module wall and stable-ID counter are hidden from the default second layer.
- English code responsibilities and paths move to the optional technical third layer.

## Current Page Exists And Is A Key Business Flow: Restyle Only, Keep Behavior

- Nine-section architecture overview, selected-section relationship map, module inspector, and return behavior keep their data and semantics.
- Clicking an individual module must still open the existing evidence-backed inspector.

## Hard Behavior Locks

- Do not change API, SSE, SkillResult, permissions, write-operation status, authentication, or other business contracts.
- Do not add fake data, fake entry points, or decorative business buttons.
- Every node must belong to exactly one visible area derived from its existing first owned path.
- Area grouping is view-only and must never be written into `SystemArchitectureSnapshot` or described as runtime order.
- Mobile must remain single-column with no horizontal overflow.
