# Scope lock

- Preserve: target-profile conceptual architecture, its profile-source disclosure, all eight configured flows, static code map, implementation drill-down, local/no-network HTML contract.
- Change: only the first-layer rendering, connector routing, labels, and ordered relation presentation.
- Do not add: runtime/deployment claims, BPMN semantics, fake systems, model calls, backend/API changes, or any source facts.
- Assets: no assets required; cards, gutters, bands, markers, and connectors are HTML/CSS/SVG.
