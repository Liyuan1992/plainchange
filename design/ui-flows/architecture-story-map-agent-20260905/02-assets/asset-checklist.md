# Asset checklist

No image asset is needed. The correction uses existing color tokens and real DOM/SVG text only. Do not generate screenshots, icons, or decorative images as substitute architecture content.
