# Touched files

- `src/change_passport/templates/review.html` — adds non-semantic visual responsibility-band containers inside the first architecture layer.
- `src/change_passport/templates/review.css` — defines band geometry, compact numbered markers, desktop gutters, relation-key grouping, and narrow-stack behavior.
- `src/change_passport/templates/review.js` — derives a topology-neutral story layout from component types, renders ordered relation groups, and routes every SVG connector through an orthogonal gutter.
- `tests/test_html_renderer.py` — guards the stable story-map markup and confirms floating conceptual edge labels do not return.
- `04-validation/capture.py` — browser regression harness that samples connector paths against card interiors and records narrow-layout evidence.

Not changed: target-profile data, Git/import extraction, system snapshot, static implementation map, baseline authority, or external integrations.
