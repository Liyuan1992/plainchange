# Touched files

| Path | Why |
| --- | --- |
| `src/change_passport/target_profile.py` | Strictly parse and serialize profile-sourced concept components, flows, layout, and static-group mapping hints. |
| `examples/target-profiles/change-passport.v1.json` | Declare the eight-component Change Passport workflow and human approval boundary. |
| `src/change_passport/review_model.py` | Derive a labelled concept-map view while preserving its profile source and keeping it separate from static topology. |
| `src/change_passport/templates/review.html` | Put system workflow before the existing implementation layer. |
| `src/change_passport/templates/review.css` | Add responsive concept-card, arrow, source-label, and implementation-layer styles. |
| `src/change_passport/templates/review.js` | Render the concept flow and allow mapped components to enter the existing static-code drill-down. |
| `tests/test_target_profile.py`, `tests/test_html_renderer.py` | Cover strict concept mappings, source label, and stable HTML layer headings. |
