# Scope Lock

## Reference Exists And Current Business Exists: Must Implement

- Existing nine verified system sections, selected-section inspector, group-edge counts, and module drill-down.
- The user's reported problem area: intersecting/hidden SVG links and ambiguous relationship direction.

## Reference Exists But Current Business Does Not Exist: Do Not Fake, Pending User Confirmation

- None. The correction uses only existing group edges and controls.

## Current Page Exists But Reference Does Not Show It: Hide Or Downplay This Task

- The full relation list below the fold stays intact; it is not the primary correction surface.
- Non-related sections are visually downplayed only while focusing a selected section/relation.

## Current Page Exists And Is A Key Business Flow: Restyle Only, Keep Behavior

- Top-level tab switching, section selection, module expansion, module inspection, Escape return, and scroll restoration.
- Candidate snapshot scope warning, coverage counts, changed-section markers, and static-import limitations.

## Hard Behavior Locks

- Do not change API, SSE, SkillResult, permissions, write-operation status, authentication, or other business contracts.
- Do not add fake data, fake entry points, or decorative business buttons.
- Do not change snapshot/group/edge identity or relationship counts.
- Every visual line and relation control must resolve to an existing `group_edge_id`.
- Keep mobile SVG disabled and provide equivalent text controls.
