# Visual Spec

## Metadata

- Target page: review technical architecture
- Target mode: agent
- Reference: `00-inputs/reference.png`
- Current before: `00-inputs/current-before.png`
- Date: 20260911

## Project Context Summary

- Current framework: offline HTML/CSS/JavaScript report generated by PlainChange.
- Current theme entry: CSS variables and architecture rules in `src/plainchange/templates/review.css`.
- Current page component structure: owner software map -> technical details disclosure -> unified architecture canvas -> conceptual component strip and static implementation expansion -> right inspector.
- Current usable resource paths: existing report templates only; no image asset is needed.
- Business flows that must not change: selecting conceptual nodes, expanding implementation, selecting relations, inspector updates, evidence labels, map topology and source data.

## 1. Overall Page Layout

- Preserve the current two-column technical workspace and page-level vertical scrolling.
- Only the conceptual component strip inside the left canvas becomes horizontally scrollable when its content is wider than the available column.

## 2. Left Sidebar Specs

- Not present on this report; no sidebar work.

## 3. Main Content Specs

- The clipped conceptual cards must remain the same size and order.
- The inner rounded canvas must expose a real horizontal scroll range rather than hiding overflow.
- Vertical page scrolling must continue outside that horizontal axis.

## 4. Right Sidebar Specs

- Preserve the sticky inspector, facts, direct-relation list and its independent vertical scrolling.

## 5. Card Specs

- Preserve current node widths, selected state, evidence badges, radii and hierarchy.

## 6. Typography Hierarchy

- No typography changes.

## 7. Colors, Backgrounds, Shadows, And Radii

- Preserve existing theme colors, grid background, shadows and border radii.
- The native thin horizontal scrollbar may use existing line and blue tokens.

## 8. Icon And Illustration Placement

- No asset or icon changes.

## 9. Existing Features To Hide Or Downplay

- Nothing is hidden or downplayed; the task restores access to already-rendered cards.

## 10. Fake Or New Features That Must Not Be Added

- Do not add navigation, zoom, drag affordances, fake controls or new architecture claims.
