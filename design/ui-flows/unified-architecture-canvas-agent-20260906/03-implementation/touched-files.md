# Touched Files

| File | Reason | Round |
| --- | --- | --- |
|  |  |  |
# Touched files

| Path | Why |
| --- | --- |
| `src/change_passport/templates/review.html` | Move the conceptual and selected static implementation surfaces into one workspace and remove below-canvas drill-down containers. |
| `src/change_passport/templates/review.js` | Turn mapped workflow cards into direct expand controls, render selected subdomains inline, and make the right inspector the explanation and optional source-module surface. |
| `src/change_passport/templates/review.css` | Add unified-canvas, inline-expansion, and inspector module-disclosure layout rules using existing tokens. |
| `tests/test_html_renderer.py` | Lock the unified explorer's unconditional HTML contract. |
| `src/change_passport/templates/review.css` | Round 2: make every conceptual topology read in a single top-to-bottom column and suppress decorative arrows that could imply a false linear relationship. |
