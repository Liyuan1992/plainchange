# Round 2

- Focus: reading order and generic diagram truthfulness.
- Review item: owner found the horizontal first-level map harder to scan than a top-to-bottom explanation.
- Change: force the unified architecture reader into a single workflow column at every width; keep the deterministic component order and stage badges, expand selected static implementation below the workflow map, and use the complete relationship list rather than decorative canvas arrows.
- Why this is generic: it does not inspect a repository identity or topology. A branching, merging, cyclic, or linear profile all receive the same vertical reader and retain their complete declared relationship text; no artificial sequential connector claims that one card necessarily causes the next.
- Verification result: pending regenerated FastAPI and self-hosted artifacts plus desktop and narrow live browser checks.
- Remaining deviation: this improves visual reading order, not semantic comprehension proof; independent beginner retelling remains separately required.
