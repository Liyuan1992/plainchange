# TASK-20260912-065：用 VideoFactory 回归负责人验证控制面

状态：DONE
风险等级：标准
日期：2026-09-12
领域：负责人体验、跨项目验证

## 目标与授权

用户认可 PlainChange 自分析报告后要求“换一个”项目验证。选择业务流程和媒体
产物都明显不同的 VideoFactory 固定提交
`58d2149..d6594e3`，检验负责人控制面能否独立说明画面变化、过期结果失效、
已完成检查和实际验收边界。授权不包含修改 VideoFactory、提交或推送。

## 执行边界

- 源项目有大量未提交工作，只从本地克隆到独立实验目录并固定到目标提交。
- 不为 VideoFactory、渲染、QA 或文件名增加 PlainChange 专用规则。
- 测试、构建和报告生成均在隔离副本或 PlainChange artifacts 中进行。
- 测试收据只能证明固定范围；自动测试不替代最终成片视觉验收。

## 结果

- 模型报告的一句话结论是“背景更有层次，旧成片和 QA 结果会失效”。
- 已验证区显示 151 项完整自动测试、27 项渲染失效/画面调色专项测试和
  Python 发布包构建，全部通过。
- 剩余边界明确为真实项目的最终画面、旧结果失效提示和制作人员实际体验，
  验证方法是生成真实视频并进行人工视觉与交付检查，责任方是产品负责人或
  真实使用者。
- 回归发现并修复 BUG-20260912-058：有明确 `next_check` 的已确认/推断型
  attention 结论此前不会进入剩余验证控制面。

## 验证

- VideoFactory 固定副本：151 项测试通过。
- VideoFactory 相关测试：27 项通过。
- VideoFactory：sdist 与 wheel 构建成功。
- PlainChange：155 项测试通过，sdist、wheel、Windows EXE 和 ZIP 构建成功。
- 最终报告：`artifacts/videofactory-owner-verification-v2/review.html`，
  3 项通过、0 项失败、1 项剩余边界。
- 所有会写入的测试、依赖和构建命令均在隔离副本运行；源 VideoFactory 只做
  Git 读取，HEAD 在结束时仍为 `d6594e3`。源仓库的未提交条目在本轮期间从
  73 变为 75，无法归因于本任务，因此不把“工作树完全未变化”写成已验证
  事实。隔离副本的 tracked worktree 为干净状态。

## 未验证

- 没有真实重新渲染一条视频并进行人工视觉验收。
- 没有验证其他 VideoFactory 提交或其他媒体生产项目。
- 本次跨项目成功是一个回归样本，不足以建立普遍效果结论。
